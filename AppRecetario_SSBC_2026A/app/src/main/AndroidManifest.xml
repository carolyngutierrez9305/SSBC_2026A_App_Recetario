<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppRecetario_SSBC_2026A">
        <activity
            android:name=".DetalleReceta"
            android:exported="false" />
        <activity
            android:name=".activity_detalle_receta"
            android:exported="false" />
        <activity
            android:name=".AgregarRecetaActivity"
            android:exported="false" />
        <activity
            android:name=".PantallaSplash"
            android:exported="true"
            android:theme="@style/TemaSplash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>